{
  "name": "havilering",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "prepare:wasm": "mkdir -p public/wasm && cp node_modules/onnxruntime-web/dist/ort-wasm-*.wasm public/wasm/ && cp node_modules/onnxruntime-web/dist/ort-wasm-*.mjs public/wasm/",
    "build:pages": "npm run prepare:wasm && npm run build"
  },
  "keywords": [],
  "author": "Andy Bond",
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "vite": "^6.3.5",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "onnxruntime-web": "^1.23.2"
  }
}
